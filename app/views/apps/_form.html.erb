<%= form_for(app) do |f| %>
  <% if app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(app.errors.count, "error") %> prohibited this app from being saved:</h2>
    </div>
  <% end %>

  <div class="field<%= ' field_with_errors' if app.errors[:name].any? %>">
    <span class="error"><%= app.errors.messages[:name].join(". ") if app.errors[:name].any? %></span>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
